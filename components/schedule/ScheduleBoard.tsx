/**
 * @deprecated Replaced by the continuous-scroll schedule:
 *   ScheduleScreen + ScheduleProvider + WeekSection + WeekControl.
 * Kept as an empty module only so nothing dangles; safe to delete.
 */
export {};
