# _TRASH

Files moved here during the repo janitor pass.

**Safe to permanently delete via Windows Explorer** when you're satisfied nothing valuable was lost.

Path to nuke: `C:\Users\Soy\Documents\Repos\Pare\docs\_TRASH\`

## Why each thing is here

| File | Reason |
|---|---|
| `port-to-react-native.md` | Path you didn't take (decided WKWebView wrapper instead) |
| `audit-empty-folder/` | Old duplicate of `docs/audits/` — its 4 files were moved into audits/ with prefix `2025-early-` |
| `specs-empty-folder/` | Single orphan file moved into audits/ |
| `archive-*.{md,txt,rtf}` | Old planning docs replaced by repo CLAUDE.md, docs/mobile/, and CHANGELOG.md |
| `data-pfr-*.html` | Raw HTML scrapes — superseded by the .csv exports next to them |
| `debug-*.ts` | Dev debugging utilities — verified no imports |
| `public-svg-boilerplate/*.svg` | Default svgs from `create-next-app` template — verified unused |
